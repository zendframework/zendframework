<?xml version="1.0" encoding="utf-8"?>
<section xmlns="http://docbook.org/ns/docbook" version="5.0" xml:id="zend.json.basics"><info><title>שימוש בסיסי</title></info>
    
    <para>
        שימוש ב <code>Zend_Json</code> נעשה על ידי קריאה לשני מתודות סטטיות שהם:
        <code>Zend_Json::encode()</code> ו <code>Zend_Json::decode()</code>.
    </para>
    <programlisting role="php"><![CDATA[
// Retrieve a value:
$phpNative = Zend_Json::decode($encodedValue);

// Encode it to return to the client:
$json = Zend_Json::encode($phpNative);

    ]]></programlisting>
</section>
