<sect1 id="zend.mail.character-sets">
    <title>Set dei caratteri</title>
    <para>
        <code>Zend_Mail</code> non esegue alcun controllo sulla correttezza dei set di caratteri delle parti dell'e-mail.
        Quando si crea un'istanza di <code>Zend_Mail</code> è possibile specificare un set di caratteri per l'e-mail. Il valore predefinito è <code>iso-8859-1</code>.
        L'applicazione deve verificare che tutte le parti aggiunte all'oggetto e-mail siano codificate nel set di caratteri corretto.
        In fase di creazione di una nuova parte dell'e-mail è possibile indicare un set di caratteri differente per ogni parte.
    </para>
    <note>
        <title>Solo per il formato testo</title>
        <para>
            Il set di caratteri è applicabile esclusivamente alle parti del messaggio in formato testuale.
        </para>
    </note>
</sect1>
<!--
vim:se ts=4 sw=4 et:
-->