<?php
$isContainer = $this->container instanceof Zend_Navigation_Container
             ? 'yes'
             : 'no';

$pages = array();
foreach ($this->container as $page) {
    $pages[] = $page->getLabel();
}
$pages = implode(', ', $pages);
?>
Is a container: <?php echo $isContainer . "\n"; ?>
Pages: <?php echo $pages; ?>